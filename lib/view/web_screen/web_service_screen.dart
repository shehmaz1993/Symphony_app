import 'package:flutter/material.dart';
class WebServiceScreen extends StatelessWidget {
  const WebServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('web service'),
      ),
    );
  }
}
